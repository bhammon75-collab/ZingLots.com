project_id = "huebxglhbenulbcftzdq"
[api]
# Schemas to expose in the API
schemas = ["public", "graphql_public", "app"]

# Extra search path for PostgREST
extra_search_path = ["public", "extensions", "app"]

# (optional, but recommended)
max_rows = 1000

[functions.ping]
enabled = true
verify_jwt = true
import_map = "./functions/ping/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/ping/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/ping/*.html" ]
